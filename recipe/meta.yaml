package:
    name: psrchive
    version: {{ os.environ.get('VERSION') }} 

source:
    git_url: ../psrchive
        
build:
    number: 1

test:
    imports:
        - psrchive

requirements:
    build:
        - {{ compiler('cxx') }}
        - {{ compiler('c') }}
        - {{ compiler('fortran') }}
          #- {{ cdt('xorg-x11-proto-devel') }}
          #- {{ cdt('libx11-devel') }}
          #- {{ cdt('libxcb') }}
          #- {{ cdt('libxau') }}
        - autoconf
        - automake
        - libtool
        - swig
    host:
        - cfitsio
        - pgplot
        - gsl
        - python {{ python }}
        - numpy
        - fftw
        - epsic
    run:
        - cfitsio
        - pgplot
        - gsl
        - python
        - numpy
        - tempo
        - fftw
        - epsic
